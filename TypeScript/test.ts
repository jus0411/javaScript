console.log("이게 타입스크립트다!");

const cookie1 = {
    name : "화이트쿠키",
    price : 2500,
};

console.log(`${cookie1.name}은 가격이 ${cookie1.price}원이다`);
// npm[실행해줘][설치해줘]
// npm install -g typescript
// npm install -g ts-node